name: Keepalive

on:
  schedule:
    - cron: '*/4 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health
        run: curl -f ${{ secrets.BACKEND_URL }}/health || echo "Backend unreachable"
